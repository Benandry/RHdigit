<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity 
        name="App\EmployeManagement\Domain\Model\Entity\Departement"
        table="departement"
        repository-class="App\EmployeManagement\Infrastructure\Persistence\Doctrine\Orm\DepartementOrmRepository"
    >

        <!-- ID -->
        <id name="id" type="integer" column="id">
            <generator strategy="AUTO"/>
        </id>

        <!-- Fields -->
        <field name="name" type="string" length="255" nullable="true"/>
        <field name="description" type="text" nullable="true"/>
        <field name="createdAt" type="datetime_immutable"/>
        <field name="updatedAt" type="datetime_immutable" nullable="true"/>

        <!-- Relation OneToMany -->
        <one-to-many field="postes"
                     target-entity="App\EmployeManagement\Domain\Model\Entity\Poste"
                     mapped-by="departement"
                     orphan-removal="true">
        </one-to-many>

        <!-- Lifecycle Callbacks -->
        <lifecycle-callbacks>
            <lifecycle-callback type="preUpdate" method="markUpdated"/>
        </lifecycle-callbacks>

    </entity>
</doctrine-mapping>