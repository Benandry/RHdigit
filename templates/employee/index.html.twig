{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	employee
{% endblock %}

{% block body %}
	<div class="w-full px-4">
		<div class="flex justify-between items-center mb-6">
			<h1 class="text-2xl font-bold text-gray-800">Gestion des Employés</h1>
			<a href="{{ path('app_employee.create') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition duration-300 flex items-center gap-2">
				<i class="fas fa-plus"></i> Créer nouveau
			</a>
		</div>
		<div class="bg-white rounded-xl shadow-sm overflow-hidden w-full">
			<table class="w-full divide-y divide-gray-200">
				<thead class="bg-gray-50">
					<tr>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Nom</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Prénom</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Cin</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Date de naissance</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Adresse</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Téléphone</th>
						<th class="px-6 py-3 text-left font-medium text-gray-500 uppercase tracking-wider">Actions</th>
					</tr>
				</thead>
				<tbody>
					{% for employee in employees %}
						<tr>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.firstname }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.lastname }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.cin }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.dateOfBirth ? employee.dateOfBirth|date('Y-m-d') : '' }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.adresse }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900">{{ employee.phoneNumber }}</td>
							<td class="px-6 py-4 whitespace-nowrap text-gray-900 space-x-2 flex items-center">
								<a href="{{ path('app_employee.show', {'id': employee.id}) }}" class="text-blue-600 hover:text-blue-900 inline-flex items-center gap-1">
									<i class="fas fa-eye"></i>
								</a>
								<a href="{{ path('app_employee.edit', {'id': employee.id}) }}" class="text-green-600 hover:text-green-900 inline-flex items-center gap-1">
									<i class="fas fa-edit"></i>
								</a>
								{{ include('employee/_delete_form.html.twig') }}
							</td>
						</tr>
					{% else %}
						<tr>
							<td colspan="7" class="px-6 py-4 text-center text-gray-500">Aucun enregistrement trouvé</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</div>
</div>{% endblock %}
