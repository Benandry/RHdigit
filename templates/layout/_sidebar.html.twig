<aside class="w-sm bg-gradient-to-br from-gray-800 to-blue-900 shadow-xl left-0 h-screen border-r border-gray-700 z-50">
    <div class="p-6">
        <h2 class="text-white text-xl font-bold truncate">
            RH<span class="text-blue-400">digit</span>
        </h2>
    </div>

    {% set current_path = app.request.get('_route') %}
    {% set rh_items = {
        'app_employee.index': ['Employés', 'fas fa-user'],
        'app_departement.index': ['Département', 'fas fa-building'],
        'app_contrat.index': ['Contrat', 'fas fa-file-contract'],
        'app_poste.index': ['Poste', 'fas fa-briefcase']
    } %}
    {% set rh_routes = rh_items|keys %}

    <nav class="mt-6 px-4" x-data="{ 
            openRH: {{ current_path in rh_routes ? 'true' : 'false' }} 
        }">
        <div class="space-y-2">

            <!-- Dashboard -->
            <a href="{{ path('app_home') }}"
               class="flex items-center gap-3 px-4 py-3 {{ current_path == 'app_home' ? 'bg-blue-900/30 text-blue-300' : 'text-gray-300'}} rounded-lg hover:bg-gray-800/30 hover:text-white transition">
                <i class="fas fa-home h-5 w-5"></i>
                <span class="font-medium truncate">Tableau de bord</span>
            </a>

            <!-- Groupe RH -->
            <div>
                <button 
                    @click="openRH = !openRH"
                    class="w-full flex items-center justify-between px-4 py-3 text-gray-300 hover:bg-gray-800/30 hover:text-white rounded-lg transition">
                    
                    <div class="flex items-center gap-3">
                        <i class="fas fa-layer-group h-5 w-5"></i>
                        <span class="font-medium truncate" title="Ressources humaines">Ressources humaines</span>
                    </div>

                    <i class="fas fa-chevron-down inline-block"
                        :style="openRH ? 'transform: rotate(0deg); transition: transform 0.3s ease-in-out;' : 'transform: rotate(-90deg); transition: transform 0.3s ease-in-out;'"></i>
                </button>

                <!-- Dropdown -->
                <div x-show="openRH"
                     x-transition:enter="transition ease-out duration-300"
                     x-transition:enter-start="opacity-0 scale-y-90 max-h-0"
                     x-transition:enter-end="opacity-100 scale-y-100 max-h-96"
                     x-transition:leave="transition ease-in duration-200"
                     x-transition:leave-start="opacity-100 scale-y-100 max-h-96"
                     x-transition:leave-end="opacity-0 scale-y-90 max-h-0"
                     class="overflow-hidden pl-10 mt-2 space-y-1">

                    {% for route, data in rh_items %}
                        <a href="{{ path(route) }}"
                           class="flex items-center gap-2 px-4 py-2 rounded-lg text-lg {{ current_path starts with route|split('.')|first ~ '.' ? 'bg-blue-900/30 text-blue-300' : 'text-gray-400'}} hover:bg-gray-800/30 hover:text-white transition truncate"
                           title="{{ data[0] }}">
                            <i class="{{ data[1] }} w-4 h-4"></i>
                            <span>{{ data[0] }}</span>
                        </a>
                    {% endfor %}
                </div>
            </div> 

            <!-- Congé -->
            <a href="{{ path('app_leave.index') }}"
               class="flex items-center gap-3 px-4 py-3 {{ current_path starts with 'app_leave.' ? 'bg-blue-900/30 text-blue-300' : 'text-gray-300'}} hover:bg-gray-800/30 hover:text-white rounded-lg transition truncate">
                <i class="fas fa-calendar-check h-5 w-5"></i>
                <span class="font-medium">Gestion de congé</span>
            </a>

        </div>

        <!-- User -->
        <div class="absolute bottom-0 w-full left-0 p-4">
            <div class="flex items-center gap-3">
                <div class="rounded-full w-10 h-10 ring-2 ring-blue-400 flex items-center justify-center">
                    <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewbox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                              d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                    </svg>
                </div>
                <div class="truncate">
                    <p class="text-white font-medium truncate">{{ app.user.username }}</p>
                    <p class="text-gray-400 truncate">{{ app.user.email }}</p>
                </div>
            </div>
        </div>
    </nav>
</aside>